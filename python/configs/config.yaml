# ============================================
# StockBuddy Configuration System
# ============================================
# Configuration Priority (highest to lowest):
# 1. Environment Variables (highest priority)
# 2. .env file 
# 3. YAML files (lowest priority, system defaults)
# ============================================

# Model Provider Configuration
models:
  # Primary provider
  # 设置为 "auto" 启用智能自动选择
  # 或指定具体 provider: "openai", "openrouter", "moonshot", "google"
  primary_provider: "auto"
  
  # 🆕 自动选择配置
  auto_selection:
    enabled: true              # 启用自动选择
    check_health: true         # 启用健康检查（检测网络可达性）
    cache_duration: 300        # 缓存时长（秒）
    region: "auto"             # 区域: auto, cn_no_vpn, cn_with_vpn, international
  
  # Global default settings
  defaults:
    # temperature: 0.7
    # max_tokens: 4096
  
  # Provider registry
  providers:
    openrouter:
      config_file: "providers/openrouter.yaml"
      api_key_env: "OPENROUTER_API_KEY"
    
    siliconflow:
      config_file: "providers/siliconflow.yaml"
      api_key_env: "SILICONFLOW_API_KEY"
    
    google:
      config_file: "providers/google.yaml"
      api_key_env: "GOOGLE_API_KEY"
    
    openai:
      config_file: "providers/openai.yaml"
      api_key_env: "OPENAI_API_KEY"
    
    moonshot:
      config_file: "providers/moonshot.yaml"
      api_key_env: "MOONSHOT_API_KEY"
    
    openai-compatible:
      config_file: "providers/openai-compatible.yaml"
      api_key_env: "OPENAI_COMPATIBLE_API_KEY"
    
    azure:
      config_file: "providers/azure.yaml"
      api_key_env: "AZURE_OPENAI_API_KEY"
      endpoint_env: "AZURE_OPENAI_ENDPOINT"

# Agent Configuration
agents:
  super_agent:
    config_file: "agents/super_agent.yaml"
  
  research_agent:
    config_file: "agents/research_agent.yaml"
  
  auto_trading_agent:
    config_file: "agents/auto_trading_agent.yaml"

